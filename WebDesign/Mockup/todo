Add Filter Settings Page
Add modal for upload OTA update file to confirm. Possibly add modal to other buttons
Add channel volume control
Add channel mute
Add full band eq
Set driver position
Add reset config button
Add config options for what each encoder controls
Add config options for what encoder sw does (single, double, hold)

Better auth system since right now we can just open console and unhide main window

Note - all commands will need to be reverse engineered r.e. protocol on RS485 bus

Note - for supporting OTA, we want the HTML/CSS/JS to be stored in the FW file itself. This means
we won't have to update the SPIFFS partition in the case of an updated layout / interface
